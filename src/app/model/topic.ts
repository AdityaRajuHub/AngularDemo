export class Topic {
    id: number;
    name: string;
    author: string;

    constructor(id, name, author) {
        this.id= id;
        this.name= name;
        this.author= author;
    }
}